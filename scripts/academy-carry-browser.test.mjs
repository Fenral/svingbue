import test from'node:test';import assert from'node:assert/strict';import{createServer}from'node:http';import{createRequire}from'node:module';import{readFile}from'node:fs';import{dirname,extname,resolve,sep}from'node:path';import{fileURLToPath}from'node:url';import{createAcademySeed}from'../academy-store.js';
const require=createRequire(import.meta.url),{chromium,webkit}=require('../tools/node_modules/playwright-core'),ROOT=resolve(dirname(fileURLToPath(import.meta.url)),'..'),WEBKIT=process.env.FG_ENGINE==='webkit'||process.argv.includes('--project=webkit'),mime=f=>({'.html':'text/html; charset=utf-8','.js':'text/javascript; charset=utf-8','.css':'text/css; charset=utf-8','.svg':'image/svg+xml','.woff2':'font/woff2'}[extname(f)]||'application/octet-stream');let server,browser,baseUrl;
test.before(async()=>{server=createServer((req,res)=>{const path=decodeURIComponent(new URL(req.url,'http://x').pathname).replace(/^\/+/, '')||'index.html',file=resolve(ROOT,path),prefix=`${ROOT}${sep}`.toLowerCase();if(!`${file}${sep}`.toLowerCase().startsWith(prefix)){res.writeHead(403).end();return;}readFile(file,(err,data)=>{if(err){res.writeHead(404).end();return;}res.writeHead(200,{'Content-Type':mime(file),'Cache-Control':'no-store'});res.end(data);});});await new Promise((ok,fail)=>{server.once('error',fail);server.listen(0,'127.0.0.1',ok);});baseUrl=`http://127.0.0.1:${server.address().port}`;browser=WEBKIT?await webkit.launch({headless:true}):await chromium.launch({channel:'msedge',headless:true}).catch(()=>chromium.launch({channel:'chrome',headless:true}));});test.after(async()=>{await browser?.close();await new Promise(ok=>server?.close(ok));});
function master(seed,id){const s=seed.experiences[id];s.status='mastered';s.acceptedAttemptId=`seed:${id}`;s.masteredAt='2026-07-15T10:00:00Z';s.evidence.knowledgeBestCorrect=5;s.evidence.knowledgeTotal=5;s.evidence.liveTransferPassed=true;s.evidence.liveTransferEvidence={seed:true};return seed;}function readySeed(){return master(createAcademySeed(),'speed-transfer');}function masterySeed({xp=0}={}){const seed=readySeed();seed.xp=xp;const s=seed.experiences.carry;s.surface=4;s.unlockedSurfaces=[0,1,2,3,4];s.startedAt='2026-07-15T10:00:00Z';return seed;}
async function open(hash='#/experience/carry',viewport={width:430,height:932},seed=null,reducedMotion='no-preference'){const context=await browser.newContext({viewport,deviceScaleFactor:1,reducedMotion}),page=await context.newPage(),errors=[];page.on('pageerror',e=>errors.push(`pageerror: ${e.message}`));page.on('console',m=>{if(m.type()==='error')errors.push(`console: ${m.text()}`);});if(seed)await page.addInitScript(v=>{if(!localStorage.getItem('strikearc.academy.v1'))localStorage.setItem('strikearc.academy.v1',JSON.stringify(v));},seed);await page.goto(`${baseUrl}/academy.html${hash}`,{waitUntil:'networkidle'});return{context,page,errors};}const setRange=(page,f,v)=>page.locator(`[data-live-field="${f}"]`).evaluate((el,next)=>{el.value=String(next);el.dispatchEvent(new Event('input',{bubbles:true}));},v);async function answerMastery(page,a=[2,0,2,1]){for(let i=0;i<4;i++){await page.locator(`[data-mastery-answer="${a[i]}"]`).click();await page.locator('[data-action="next"]').click();}}
test('canonical preview and Carry Total aliases use one native range renderer',async()=>{const{context,page,errors}=await open();assert.equal(await page.locator('#carryExperience').count(),1);assert.match(await page.locator('.low-point__prereq').textContent(),/PREVIEW/);assert.match(await page.locator('.carry__plot').textContent(),/174\.25 YD.*ILLUSTRATIVE TOTAL/s);for(const[alias,title]of[['carry','Carry'],['total','Illustrative Total']]){await page.evaluate(v=>location.hash=`#/lesson/${v}`,alias);await page.locator('[data-sheet-title]').filter({hasText:title}).waitFor();await page.locator('[data-sheet-close]').click();}assert.deepEqual(errors,[]);await context.close();});
test('S0–S3 prove endpoint speed equal Carry full denominator roll and real boundary',async()=>{const{context,page,errors}=await open('#/experience/carry',{width:430,height:932},readySeed(),'reduce');const l=page.locator('#carryExperience');await l.locator('[data-primary]').click();for(let i=0;i<4;i++){await l.locator(`[data-range-step="${i}"]`).click();await l.locator('[data-range-answer="0"]').click();}assert.match(await l.textContent(),/CARRY PLANE \+ ILLUSTRATIVE TAIL VERIFIED/);await l.locator('[data-action="next"]').click();for(const s of['equation','sweep','equal','real','boundary'])await l.locator(`[data-influence="${s}"]`).click();assert.match(await l.textContent(),/MODEL FIT BOUNDARY.*CORE CONTROLS REMAIN BELOW 160 MPH/s);await l.locator('[data-action="next"]').click();for(let i=0;i<5;i++){await l.locator('[data-myth-answer="1"]').click();await l.locator('[data-action="next"]').click();}assert.equal(await l.getAttribute('data-surface'),'4');const audit=await page.evaluate(()=>({overflow:document.documentElement.scrollWidth>document.documentElement.clientWidth,min:Math.min(...[...document.querySelectorAll('#carryExperience button,#carryExperience input')].filter(x=>x.offsetParent).map(x=>x.getBoundingClientRect().height))}));assert.equal(audit.overflow,false);assert.ok(audit.min>=44);assert.deepEqual(errors,[]);await context.close();});
test('knowledge without live equal-Carry proof remains Practiced',async()=>{const{context,page,errors}=await open('#/experience/carry/surface/4',{width:430,height:932},masterySeed({xp:20}));await answerMastery(page);await page.locator('[data-finish]').click();assert.match(await page.locator('[data-result-status]').textContent(),/needs repair/);const stored=await page.evaluate(()=>JSON.parse(localStorage.getItem('strikearc.academy.v1')));assert.equal(stored.xp,20);assert.equal(stored.experiences.carry.status,'practiced');assert.deepEqual(errors,[]);await context.close();});
test('two learner-created trajectories share one plane and earn one canonical reward',async()=>{const{context,page,errors}=await open('#/experience/carry/surface/4',{width:375,height:812},masterySeed({xp:20}));await answerMastery(page);for(const[f,v]of Object.entries({dynamicLoft:25,attackAngle:-5,clubSpeed:90}))await setRange(page,f,v);await page.locator('[data-capture]').click();for(const[f,v]of Object.entries({dynamicLoft:35,attackAngle:5,clubSpeed:90}))await setRange(page,f,v);await page.locator('[data-capture]').click();assert.equal(await page.locator('.carry__plot table tbody tr').count(),2);assert.match(await page.locator('.carry__plot figcaption').textContent(),/RAW 62\.18° \/ DISPLAY CLAMP/);await page.locator('[data-inference="equal-current-carry-does-not-prove-equal-real-carry"]').click();await page.locator('[data-submit]').click();assert.equal((await page.locator('[data-result-status]').textContent()).trim(),'Carry model understood');let stored=await page.evaluate(()=>JSON.parse(localStorage.getItem('strikearc.academy.v1'))),state=stored.experiences.carry,e=state.evidence.liveTransferEvidence;assert.equal(stored.xp,140);assert.equal(state.status,'mastered');assert.equal(Object.values(stored.rewardLedger).filter(x=>x.experienceId==='carry').length,1);assert.ok(e.launchGap>=8&&e.apexGap>=9&&e.landingGap>=12&&e.totalGap>=3);assert.equal(e.highClamp,true);assert.equal(e.inferencePassed,true);await page.reload({waitUntil:'networkidle'});stored=await page.evaluate(()=>JSON.parse(localStorage.getItem('strikearc.academy.v1')));assert.equal(stored.xp,140);assert.deepEqual(errors,[]);await context.close();});
test('both target viewports expose solid trace dashed roll table and reduced-motion parity',async()=>{for(const viewport of[{width:430,height:932},{width:375,height:812}]){const{context,page,errors}=await open('#/experience/carry',viewport,readySeed(),'reduce');const a=await page.evaluate(()=>({width:document.documentElement.scrollWidth,client:document.documentElement.clientWidth,trace:getComputedStyle(document.querySelector('.carry__trace')).strokeDasharray,roll:getComputedStyle(document.querySelector('.carry__roll')).strokeDasharray,table:document.querySelector('.carry__plot table')?.textContent,aria:document.querySelector('.carry__plot svg')?.getAttribute('aria-label'),min:Math.min(...[...document.querySelectorAll('#carryExperience button')].filter(x=>x.offsetParent).map(x=>x.getBoundingClientRect().height))}));assert.ok(a.width<=a.client+1);assert.ok(!a.trace||a.trace==='none');assert.notEqual(a.roll,'none');assert.match(a.table,/State.*Carry.*Apex.*Landing.*Roll.*Total/s);assert.match(a.aria,/Carry .* yards.*Apex.*Landing.*illustrative roll/i);assert.ok(a.min>=44);assert.deepEqual(errors,[]);await context.close();}});
